# Competitive action register (2026-09-05)

```text
STATUS = REFERENCE_SNAPSHOT
CURRENT_ARCHITECTURE_AUTHORITY = source + maintained architecture docs + live GitHub issues
COMPETITOR_FACTS = verify against source before implementation
```

Companion to `COMPETITIVE_GAP_AUDIT_2026-09-05.md` (same directory, same review base and
verification pass). Only actionable ideas appear here. "EXACT_SOURCE_EVIDENCE" names the pinned
repo, commit, path, and symbol where the idea was verified at source; rows marked `(docs-level)`
were taken from public README/release material and need a source pass before they become an
implementation basis. Nothing here authorizes code copying: every ADAPT/ADOPT is a pattern to
reimplement under Nakagawa provenance rules, and every license note must be re-verified at the
implementation commit.

Evidence keys: JNA = `jessicanataliagta/PSPRecomp@f6e7d41`; SPZ =
`sp00nznet/psprecomp@caca759`; WE = `wizardengineer/psprecomp@bd3c33e`; RF =
`N64Recomp/RecompFrontend@b1a1477`; NAK = Nakagawa review base `12bcc52`.

| IDEA | SOURCE_PROJECT | EXACT_SOURCE_EVIDENCE | ADOPT / ADAPT / REJECT / INVESTIGATE | NAKAGAWA_ARCHITECTURAL_FIT | LLE_IMPACT | LEGAL/PROVENANCE_RISK | PORTABILITY_IMPACT | USER_VALUE | IMPLEMENTATION_COST | DEPENDENCIES | EARLIEST SAFE IMPLEMENTATION | RETIREMENT/FAILURE CONDITION |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| B1 | Second-title genericity proof: drive a second real PSP title through generic core + manifest with zero un-inventoried core edits | NAK | Readiness criterion in `docs/PORTING.md`; dated census `docs/provenance/GENERICITY_CENSUS_20260826.md`; current `src/rt/recomp.c` `g_exact_hooks[]` | ADAPT | Core thesis; requires migrating `g_exact_hooks`/MEM literals to typed profile config and proving their absence for title #2 | High: replaces convenience HLE with genuine guest/measured behavior | Low (public + private routes) | Neutral (core-only) | Enables every other row | High | None beyond existing toolchain | P0, private title optional | Title #2 reaches declared milestone; core objects pass purity gate |
| B2 | Mechanical purity gate: (a) game-range literal allowlist over generic core sources; (b) object-symbol boundary audit (no defined/undefined game symbols in core objects); (c) runtime null-profile census | WE | `runtime/tools/purity_gate.sh` (literal allowlist `ALLOWED_RE`; `nm` audit of core objects; game symbols = `FUN_0xxxxxxx`, module installers, asset layer) | ADAPT | Directly replaces reliance on regex census (`tools/compat_overrides.py`) with link/object truth | High (guards destination model) | Low | Stronger generic core = cleaner profile boundary | Neutral | Enables title #2 claim | Medium | Core build with profile-zero; `nm`; existing Python gates | P0 | A new title adds a core object violating the audit; gate must fail |
| B3 | Local deterministic AOT corpus caching, digest-pinned and profile-scoped | JNA | `profiles/vcs/generated/` (~237 units + `generated_registry.cpp` + `auto_codegen_report.json`) checked in per profile | ADAPT (never public retail-derived bytes) | Caches generated guest code keyed to exact ELF identity; staleness fails closed | Neutral (AOT only) | Medium-high: Nakagawa forbids publishing mechanically translated retail output; keep cache private/local under provenance labels | Neutral | Faster + reproducible per-title builds | High | Provenance-label rules; content-addressed stamps already exist | P0 | Cache keyed to ELF SHA; verify before using as build input |
| B4 | Census drift guards on generated output (`--expect-functions`, `--expect-mid-entries`) | WE | `crates/psp-cli/src/recompile.rs:87-89,393`; `crates/psp-cli/src/report.rs:67` | ADOPT | Cheap build-time tripwire over canonical CFG/function census Nakagawa already emits | Neutral | Low | Neutral | Developer-time | Low | Canonical CFG reports in `tools/analyze.py` | P0 | Census drifts without an intended change; gate fails |
| B5 | KIRK/retail-container path: ISO → PBP → `~PSP` parse → decrypt → relocate → guest image, key bytes never in-tree | SPZ (+ NAK) | SPZ `tools/allegrexrecomp/keys.c`/`keys.h` (external key file, `kirk1` 16-byte key requested at runtime; `keys/` gitignored; callers `main.c:886,912`); container/crypto under `tools/allegrexrecomp/` — **competitor implementation evidence only; it does not establish Nakagawa's key requirements or legal suitability** | INVESTIGATE (likely ADAPT) | Unlocks `user ISO -> Play`; destination-model enabler | High (genuine guest code from retail container) | High: separate algorithm implementation from key-material handling; lawful key acquisition path unresolved (sub-questions in audit §5) | Neutral | Highest: removes manual decryption/extraction from the user story | High | Public KIRK documentation; existing `prxload.py`; private `iso.c` | P0 as investigation; implementation only after facts settled | Investigation answers the §5 sub-questions; then pilot on one retail title |
| B6 | Dynamic module / original-PRX execution at runtime (load, relocate, import/export, run genuine guest modules beyond the manifest boot set) | NAK (gap) + JNA profile module pattern | NAK `hle.c` `populate_known_module` (title-gated module load, dated census item 5); JNA `profiles/vcs/` module boot handling | ADAPT | Core destination-model work: guest middleware (libfont, PSMF-class) executes rather than being host-replaced where practical | High (original guest execution) | Low-medium (module bytes stay user-supplied) | Neutral | Runs more real PSP software; fewer host replacements to maintain | Very high | Runtime loader + relocation/import machinery; hardware oracle for module semantics | P0-P1 after B1/B2 | Title loads a runtime PRX successfully; fail-closed on unknown relocations |
| B7 | Hardware-conformance harness productization (versioned, reproducible `tools/psp_oracle` suite with firmware/manifest pins) | NAK (own moat) | NAK `tools/psp_oracle/` (protocol, run_psplink, VFPU probes); `docs/HARDWARE_ORACLE.md`; measured semantics cited in `docs/ARCHITECTURE.md` | ADAPT | Expands the "measured low-level PSP platform behavior" layer beneath HLE | High (measured layer is the destination's second rung) | Low (own harness) | Neutral | Neutral | Differentiator; correctness anchor for all other work | Medium-high | PSP hardware + PSPLINK/ARK route (authorized hardware campaign) | P0 | Probe contradicts hardware on a later console/firmware; suite must fail loud |
| B8 | Interpreter role contract: correctness floor only; extend only vs. proven gaps, never as primary strategy | NAK | `src/rt/guest_interp.h` (enumerated subset), `fixtures/cosim/README.md`, `ISSUES.md` execution-contract state, issues #116/#118/#127/#128 | ADOPT (policy) | Defends the fail-closed floor while keeping AOT + dynamic-module work ahead of interpreter breadth | Neutral (protects floor; no breadth push) | Low | Neutral | Developer-time trust | Low | None | P0 | Any proposal to widen the interpreter must first show a proven correctness hole and compare AOT/module alternatives |
| C1 | Native fast-path mechanism, conditional last resort | JNA | `include/psprecomp/runtime.hpp:113-114`; `src/runtime.cpp:1105-1148` (canonical-address map; **unregistered falls back to AOT body**); `profiles/vcs/host/vcs_native_fast_paths.cpp` (differential whole-context `memcmp`, `PSPRECOMP_NO_FAST_*`, `VALIDATE_FAST_*`); emission in profile tool `vcs_codegen_main.cpp` | ADAPT (P2, conditional) | Under audit §6 policy: optional optimization only, semantics established, generic preferred, inventoried, evidence attached, removal path | Negative risk if overused (host replaces guest); policy mitigates | Low-medium (title-specific host code must stay profile-owned) | Neutral | Perf for proven hot leaves | Medium | Existing compat inventory + codegen profile isolation tests | P2, only after B1/B2 | Any fast path whose disable-switch changes behavior, or whose differential validation diverges, is removed |
| D1 | Single canonical cross-platform build (investigate CMake+Ninja vs Meson vs Make; transition then retire duplicates) | NAK (own) | NAK `Makefile` (two-phase `all`, `CHUNK_OBJS` wildcard semantics, content-addressed stamps); `docs/ARCHITECTURE.md` | INVESTIGATE (recommend CMake+Ninja single source of truth) | Build becomes platform-neutral and IDE-friendly; generated-source handling and two-phase semantics must be preserved exactly | Neutral | Low | High (MSYS2/Unix/ARM64 toolchains) | Developer + CI time | High | CMake 3.20+; Ninja; ccache decision | P1, spike on `production-smoke` first | Parity proof on source-owned targets; Make retired, never duplicated long-term |
| D2 | Host-abstraction layer: extract Win32 (time, fs, sleep, OSK, h264, process-exit) behind narrow interfaces | NAK (own, planned) | `docs/PLATFORM_PORTABILITY.md` dependency map (Win32 APIs inside `hle.c` etc.) | ADAPT | Required for destination reach (macOS/ARM64/handheld) | Neutral | Low | High (Linux end-to-end, then ARM64) | Indirect (wider platform) | High | Existing seams: `sr_coro`, `*_unavailable.c`, `h264_null.c` | P1 | Every documented Win32 touch point behind a backend; Linux link + smoke |
| D3 | x86-64-free architectural contract now (alignment/arena/ABI assumptions audited; ARM64 compile gate as portability evidence) | NAK (own) | `docs/PLATFORM_PORTABILITY.md`; guest arena 192 MiB host allocation; `recomp.h` ABI | ADAPT | Avoids baking x86 assumptions before Apple Silicon/handheld reach | Neutral | Low | High | Strategic | Low-medium | Compile-only `clang --target=aarch64` gate | P1 | Any generic-core change requiring x86-64-only types/layouts fails review + gate |
| D4 | Frontend architecture: presenter decoupling, persisted config, in-game overlay, controller menus | RF (+ NAK gap) | RF `recompui/include/recompui/recompui.h` (RmlUi context, font registration, mod list); `recompui/include/recompui/config.h`, `program_config.h`; NAK `src/rt/gui.c` (presenter only) | ADAPT | Product shell for a consumer app; UI engine chosen by spike (audit §4b), not defaulted | Neutral | Low-medium (font/license review for UI stack) | High (SDL3 already portable) | High (polished player UI) | High | UI-engine spike matrix; config persistence format | P1 (spike now, engine adoption after) | Menus navigable by controller/keyboard/mouse; config survives restarts; mods can add UI |
| D5 | Launcher/install surface for the BYO-input consumer target (Tauri-class shell later) | open-goal launcher | `(docs-level)` public launcher description only | INVESTIGATE (defer) | Separate surface from the game process; belongs at product time, after provenance/release gates | Neutral | Medium (distribution policy) | High | High | High | Webview/Tauri-class runtime | P2 | Product definition exists; release gates open |
| D6 | New-title automation scaffold (`new-title`: manifest + plan + census/import/NID diff + expect-baselines + walkthrough) | NAK (own) + WE pattern | NAK `assets/titles/{synthetic,pspdev-phase5}.json`, `tools/title_manifest.py`, `docs/PORTING.md`; WE `games/TEMPLATE` + recompile.rs baselines | ADAPT | Turns "porting is engineering" into a supported workflow; manager generalizes off HST-only acceptance | Neutral | Low | Neutral | Enables title #2+ | Medium | Existing manifest/planner/ladder | P0-P1 | Scaffold produces a buildable profile; no core edits needed for a fresh title |
| E1 | Mod ABI designed before ossification (hooks, config schema, mod-supplied UI API, load order) | RF / Zelda64Recomp | RF `recompui/include/recompui/recompui.h` `update_mod_list(bool scan_mods)`; `(docs-level)` Zelda64Recomp mod template + drag-drop install | ADAPT (design now, ship later) | Community surface gated by title-isolation profiles; ABI must not leak guest addresses | Neutral (mods hook, not replace) | Medium (mod distribution policy; no game bytes) | High | High | Medium-high | ABI + loader + registry; UI API shape from D4 | P1 | ABI frozen before community exists is a failure; ship only after D4 + a real mod use case |
| E2 | Texture-replacement architecture: hash-keyed override keyed on VRAM/CLUT/swizzle decode in the GE sampler | Zelda64Recomp/RT64 | `(docs-level)` rt64.json texture packs, release notes | ADAPT (design sampler keys now, packs later) | Uses existing GE decode path (`ge.c`, `ge_gpu.c`) without a second renderer | Neutral | Low (override images are user content) | Neutral | Highest community value after mods | Medium | Stable sampler key derivation; PNG pack + manifest format | P1 | Pack toggle changes nothing (key instability) → design fails; fix key derivation |
| E3 | Controller UX: per-device remap/profiles, deadzone, persistence | RF | `recompinput/include/recompinput/{profiles.h,players.h,input_mapping.h,input_state.h}` | ADAPT | Rides on D4 config store; SDL3 gamepad substrate exists | Neutral | Low | High | High | Medium | D4 config persistence | P1 | Profiles survive sessions and device changes (GUID-keyed) |
| E4 | Crash/support bundles + one-command repro (route state + guest PC + save baseline → auto padscript replay) | NAK (own) + SPZ instrument | NAK crash reporter + `SR_PADSCRIPT` routes + `-SaveBase` (docs/DEBUGGING.md); SPZ `src/dispatch.c` (entry/loop/label instruments) | ADAPT | Closes the loop between crash evidence and deterministic reproduction | Neutral | Low | Neutral | High (support) | Medium | Existing visual-oracle/route machinery | P1 | A crash archive replays to the same divergence without human input |
| E5 | Runtime correctness tripwires: loop back-edge and stack-balance invariants in debug builds | SPZ | `src/dispatch.c` `psp_trace_loop`, label reachability, call budget; README stack-balance finding | ADAPT (no loop caps; repo rules apply) | Makes silent corruption classes loud without changing guest semantics | Neutral | Low | Neutral | Developer-time | Low | Debug runtime only | P1 | Tripwire fires on legitimate guest behavior → wrong invariant, remove |
| E6 | Fuzz/sanitizer CI over every user-controlled input (ELF/PRX/PBP/ISO/route/manifest/keys-format) | NAK (own) + field baseline | NAK parser fuzz targets (`test_parse_fuzz`, VFPU fuzz/mutants), Betterleaks/OSV/CodeQL present; README declares hardening campaign incomplete | ADOPT | Completes declared posture; public-safe, no game inputs | Neutral | Low | Neutral | Security claim | Medium | ASAN/UBSAN runners; seeded corpora | P1 | Campaign complete; any parser accepts hostile input without sanitizer finding |
| E7 | NID/import self-verification (registry keyed and checked against authoritative names, fail-closed on mismatch) | SPZ | SHA-1 crypto in `tools/allegrexrecomp/crypto/sha1.c`; README "every NID verified against SHA-1 of its own name" (mechanism partially source-verified) | ADAPT | Extends NAK `nid_corpus.json`/`nid_auditor` with name-keyed self-check | Neutral | Low | Neutral | Fewer silent NID misroutes | Low | NID corpus + auditor | P2 | New NID entry without a verifiable name/source fails the audit |
| E8 | Memory-map fidelity: cache-behaviour mirror semantics on the single guest arena | SPZ | `src/mem.c:85` (three cache-behaviour mirrors collapsed onto one backing store) | INVESTIGATE | NAK models guest memory through helpers over one 192 MiB arena; generic PSP semantics may require mirror-consistent access auditing | Neutral-high (fidelity layer) | Low | Neutral | Correctness for titles using mirror aliases | Medium | Memory-model research; hardware/PSP oracle checks | P2 | HST never touches the aliases but a second title does → then mandatory |
| E9 | Oracle-driven bring-up tooling: scriptable behavioral oracle + TCP debug socket for live guest inspection | WE | `runtime/src/main.cpp` (loopback 9999 debug socket); PPSSPP-as-scriptable-oracle docs | ADAPT (tooling) | NAK's hardware oracle + routes are stronger; a guest-side debug socket would aid multi-title bring-up | Neutral | Low | Neutral | Developer-time | Medium | Debug transport in runtime | P2 | Socket used by one non-HST bring-up or retired if unused |
| E10 | Wall-clock/watchdog hygiene: no-new-frame watchdog + bounded diagnostics on hang, never fabricated completion | SPZ + NAK | SPZ README bring-up instruments (wall-clock watchdog); NAK `SR_WATCHDOG_EXIT` NO-NEW-FLIP semantics | ADOPT (already aligned) | Preserves fail-closed doctrine; no loop caps or fake success | Neutral | Low | Neutral | Support quality | Low | Existing | P1 | Watchdog verdict ever treated as a hang proof rather than observation |
| E11 | Updater/release architecture (signed manifests, update channel, SBOM/notice chain) | open-goal launcher / Zelda64Recomp | `(docs-level)` release + launcher descriptions | INVESTIGATE (defer to product time) | Design doc only until provenance/release gates open; no tags/releases/assets under current rules | Neutral | High (publication policy) | High | High | High | Release-gate tooling already present (`publish_audit.py`, SBOM) | P2 | Documented; implementation only when maintainer authorizes publication |
| E12 | Dated research-snapshot + live-tracker discipline (no new static roadmap docs; Issues/Milestones are status authority) | NAK (own) | `docs/provenance/GENERICITY_CENSUS_20260826.md` convention; leadership correction (audit §Appendix C) | ADOPT (process) | Keeps architecture docs truthful; mutable state stays in the tracker | Neutral | Low | Neutral | Contributor trust | Low | GitHub tracker; generated status summaries | P0 | Any new doc that duplicates tracker state in prose is refused |
| E13 | Contributor onboarding: safe-first-PR lane, bot-assisted provenance hygiene, template PRs | NAK (own) + WE docs | NAK `CONTRIBUTING.md`, `docs/` depth; WE `docs/ADDING_A_GAME.md`, `games/TEMPLATE` | ADAPT | Lowers the process floor for safe paths without weakening gates | Neutral | Low | Neutral | Contributor growth | Medium | CI + policy tooling | P1 | First-PR time-to-merge for a docs/test-only change falls materially |
| E14 | File-format parsing depth parity on the container stack (ISO9660 read quality, PBP, XB direct) | NAK (own) | `docs/ISSUE196_DIRECT_XB.md`; private `iso.c`; `tools/extract_xb.py` + libxb pin | ADAPT | Feeds B5 (container path) and title runtime needs | Neutral | Low-medium | Neutral | Correct asset access | Medium | libxb audit pin; ISO route tests | P1 | Direct-XB containment boundary proven; ISO census parity |
