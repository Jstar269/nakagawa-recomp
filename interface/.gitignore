# Dependencies and framework output
/node_modules/
/.next/
/out/
/coverage/

# Local configuration, logs, and generated types
.env
.env.*
!.env.example
*.log
*.tsbuildinfo
/next-env.d.ts

# Local database and runtime artifacts
/db/
/prisma/migrations/
/prisma/dev.db*
/prisma/.test/
/tool-results/

# Agent instruction files regenerated by `next dev`
# (node_modules/next/dist/server/lib/generate-agent-files.js). They are tool
# output, not authored policy: the repository contract lives in the root
# AGENTS.md. Tracking them would add uncommitted churn to every dev run.
/AGENTS.md
/CLAUDE.md
